# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: wolf-os
description: A declarative, high-performance Nix-style Fedora Atomic workstation.
base-image: quay.io/fedora/fedora-silverblue
image-version: 44

modules:
  # 1. First, register the repositories (Transaction 1)
  - from-file: ../modules/repositories.yml

  # 2. Second, install the software (Transaction 2)
  - from-file: ../modules/packages.yml

  # 3. Third, run the assembly script (Post-install logic)
  - type: script
    scripts:
      - setup-wolf-os.sh
